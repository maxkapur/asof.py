# asof.py

Get latest version of a Python package (PyPI, conda) as of a certain date.

## Installation

Still in development mode for now:

```shell
python -m venv ./venv
# Activate the venv, then:
python -m pip install --editable .[dev]
```
